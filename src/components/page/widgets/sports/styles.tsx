import { styled } from "@/lib/theme";

export const SportsLink = styled("a", {
	textDecoration: "none",
	display: "flex",
	justifyContent: "center",
	alignItems: "center",
	width: "$full",
	height: "$full",
});
